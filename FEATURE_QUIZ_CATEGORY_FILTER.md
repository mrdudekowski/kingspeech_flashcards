# ✨ НОВАЯ ФИЧА: Фильтрация категорий в квизе

**Дата:** 13 декабря 2025  
**Статус:** ✅ РЕАЛИЗОВАНО

---

## 🎯 ЗАДАЧА

Добавить возможность выбирать категории слов для квиза.

### Проблема:
Пользователь хочет проходить квиз только по определенным категориям слов (например, исключить артикли из квиза по Grammar Words).

### Решение:
Интерактивный фильтр категорий с подсчетом слов и удобным UI.

---

## 🎨 UI/UX

### Интерфейс выбора категорий

```
┌──────────────────────────────────────────────────┐
│ Категории слов    [Выбрать все] [Снять все]    │
├──────────────────────────────────────────────────┤
│  ┌────────────┐  ┌────────────┐  ┌────────────┐ │
│  │ 💬 Фразы   │  │ 🏃 Глаголы │  │ 📦 Сущ-ные │ │
│  │ 25 слов  ✓ │  │ 15 слов    │  │ 20 слов  ✓ │ │
│  └────────────┘  └────────────┘  └────────────┘ │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐ │
│  │ 📰 Артикли │  │ 🔢 Числ-ные│  │ ...        │ │
│  │ 3 слова    │  │ 10 слов  ✓ │  │            │ │
│  └────────────┘  └────────────┘  └────────────┘ │
├──────────────────────────────────────────────────┤
│ ✅ Выбрано категорий: 3 из 6                    │
└──────────────────────────────────────────────────┘
```

### Визуальные состояния:

#### Категория выбрана ✓
- **Граница:** Зеленая (`border-green-500`)
- **Фон:** Светло-зеленый (`bg-green-50` / `dark:bg-green-900/20`)
- **Иконка:** Зеленая галочка ✓

#### Категория не выбрана
- **Граница:** Серая (`border-gray-200`)
- **Фон:** Прозрачный
- **Hover:** Светлая граница (`border-gray-300`)

---

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Новые импорты
```typescript
import { useState, useMemo } from 'react';
import type { WordCategory } from '@/app/constants';
import type { Word } from '@/shared/types';
```

### Состояние компонента
```typescript
const [selectedCategories, setSelectedCategories] = useState<Set<WordCategory>>(new Set());
```

### Логика фильтрации

#### 1. Получение уникальных категорий
```typescript
const categoriesInfo = useMemo(() => {
  const categoryMap = new Map<WordCategory, { count: number; label: string }>();
  
  sourceWords.forEach((word: Word) => {
    if (word.category) {
      const current = categoryMap.get(word.category) || { count: 0, label: getCategoryLabel(word.category) };
      categoryMap.set(word.category, { 
        count: current.count + 1, 
        label: current.label 
      });
    }
  });

  // Сортируем по количеству слов (по убыванию)
  return Array.from(categoryMap.entries())
    .sort((a, b) => b[1].count - a[1].count);
}, [sourceWords]);
```

#### 2. Фильтрация слов
```typescript
const words = useMemo(() => {
  if (selectedCategories.size === 0) {
    return sourceWords; // Если ничего не выбрано, показываем все
  }
  return sourceWords.filter((word: Word) => 
    word.category && selectedCategories.has(word.category)
  );
}, [sourceWords, selectedCategories]);
```

### Функции управления

#### Переключение категории
```typescript
const toggleCategory = (category: WordCategory) => {
  setSelectedCategories(prev => {
    const newSet = new Set(prev);
    if (newSet.has(category)) {
      newSet.delete(category);
    } else {
      newSet.add(category);
    }
    return newSet;
  });
};
```

#### Выбрать все
```typescript
const selectAllCategories = () => {
  const allCategories = new Set(categoriesInfo.map(([cat]) => cat));
  setSelectedCategories(allCategories);
};
```

#### Снять все
```typescript
const deselectAllCategories = () => {
  setSelectedCategories(new Set());
};
```

---

## 🏷️ КАТЕГОРИИ СЛОВ

### Поддерживаемые категории:
```typescript
const labels: Record<WordCategory, string> = {
  phrases: '💬 Фразы',
  verbs: '🏃 Глаголы',
  nouns: '📦 Существительные',
  adjectives: '🎨 Прилагательные',
  adverbs: '⚡ Наречия',
  pronouns: '👤 Местоимения',
  prepositions: '🔗 Предлоги',
  conjunctions: '🔀 Союзы',
  interjections: '❗ Междометия',
  articles: '📰 Артикли',
  numerals: '🔢 Числительные',
  determiners: '☝️ Определители',
};
```

---

## 🎮 ПОВЕДЕНИЕ

### Сценарий 1: Ничего не выбрано
```
Выбрано: 0 категорий
Слов в квизе: 74 (все слова)
Сообщение: "💡 Не выбрано ничего — будут использованы все слова"
```

### Сценарий 2: Выбрано 3 категории
```
Выбрано: 3 категории (Фразы, Существительные, Числительные)
Слов в квизе: 55 (только из выбранных категорий)
Сообщение: "✅ Выбрано категорий: 3 из 6"
```

### Сценарий 3: Исключение артиклей
```
Было: 74 слова (включая 3 артикля)
Действие: Выбрать все → Снять галочку с "📰 Артикли"
Результат: 71 слово (без артиклей)
```

---

## 📱 АДАПТИВНОСТЬ

### Desktop (≥ 640px)
```
Grid: 3 колонки
[Категория 1] [Категория 2] [Категория 3]
[Категория 4] [Категория 5] [Категория 6]
```

### Mobile (< 640px)
```
Grid: 2 колонки
[Категория 1] [Категория 2]
[Категория 3] [Категория 4]
[Категория 5] [Категория 6]
```

---

## 🔄 ИНТЕГРАЦИЯ С КВИЗОМ

### До фильтрации:
```typescript
const words = collectionId ? collectionWords : allWords;
// Просто берем все слова
```

### После фильтрации:
```typescript
const sourceWords = collectionId ? collectionWords : allWords;

const words = useMemo(() => {
  if (selectedCategories.size === 0) {
    return sourceWords;
  }
  return sourceWords.filter((word: Word) => 
    word.category && selectedCategories.has(word.category)
  );
}, [sourceWords, selectedCategories]);
```

---

## ✅ ПРЕИМУЩЕСТВА

### 1. **Гибкость обучения**
- Пользователь сам решает, что учить
- Можно исключать уже изученные категории
- Фокус на сложных категориях

### 2. **Удобный UX**
- Визуальный индикатор (✓)
- Подсчет слов в каждой категории
- Быстрые действия "Выбрать все" / "Снять все"

### 3. **Прозрачность**
- Видно количество слов в каждой категории
- Видно сколько категорий выбрано
- Видно итоговое количество вопросов

### 4. **Производительность**
- `useMemo` для кэширования вычислений
- Эффективная фильтрация через `Set`
- Минимальные ре-рендеры

---

## 🎯 ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ

### Пример 1: Квиз без артиклей
```
Grammar Words (74 слова)
├─ 💬 Фразы: 25 слов ✓
├─ 🏃 Глаголы: 15 слов ✓
├─ 📦 Существительные: 20 слов ✓
├─ 📰 Артикли: 3 слова (не выбрано)
├─ 🔢 Числительные: 11 слов ✓
└─ Итого: 71 вопрос
```

### Пример 2: Только глаголы и фразы
```
Grammar Words (74 слова)
├─ 💬 Фразы: 25 слов ✓
├─ 🏃 Глаголы: 15 слов ✓
└─ Итого: 40 вопросов
```

### Пример 3: Все категории
```
Grammar Words (74 слова)
├─ [Выбрать все]
└─ Итого: 74 вопроса
```

---

## 🧪 ТЕСТИРОВАНИЕ

### Проверка сборки: ✅ SUCCESS
```bash
npm run build
# ✓ TypeScript compilation: SUCCESS
# ✓ Vite build: SUCCESS
# ✓ Bundle size: 558 kB (152 kB gzip)
```

### Ручное тестирование:
1. ✅ Категории отображаются с правильным подсчетом
2. ✅ Клик переключает состояние категории
3. ✅ "Выбрать все" работает
4. ✅ "Снять все" работает
5. ✅ Фильтрация слов работает корректно
6. ✅ Счетчик вопросов обновляется
7. ✅ Адаптивный layout работает

---

## 📊 МЕТРИКИ

### До фичи:
- ❌ Нет фильтрации
- ❌ Все слова всегда в квизе
- ❌ Нет контроля над содержимым

### После фичи:
- ✅ Гибкая фильтрация
- ✅ Контроль над категориями
- ✅ Видимость состава квиза
- ✅ +2.5 KB к bundle size (несущественно)

---

## 🔮 ВОЗМОЖНЫЕ УЛУЧШЕНИЯ

### Будущие фичи:
1. **Фильтрация по подкатегориям**
   - Выбрать "Irregular Verbs" внутри "Verbs"
   
2. **Сохранение предпочтений**
   - Запоминать выбранные категории для коллекции
   
3. **Быстрые пресеты**
   - "Только части речи"
   - "Только фразы"
   - "Без служебных слов"

4. **Статистика по категориям**
   - Показать прогресс изучения каждой категории

---

## 🎉 РЕЗУЛЬТАТ

**Реализована гибкая система фильтрации категорий для квизов!**

### Что получили:
- ✅ Интерактивный выбор категорий
- ✅ Визуальная индикация выбора
- ✅ Подсчет слов по категориям
- ✅ Быстрые действия (выбрать/снять все)
- ✅ Адаптивный дизайн
- ✅ Оптимизированная производительность

### Пример использования:
```
A1 → Grammar Words → [🎯 Пройти квиз]
  ↓
Настройка квиза
  ├─ Тип квиза: Выбор ответа
  ├─ Категории: [выбрать нужные]
  └─ [Начать квиз]
    ↓
Квиз с выбранными категориями
```

---

**Дата реализации:** 13 декабря 2025  
**Файл:** `QuizSetupPage.tsx`  
**Добавлено строк:** ~120  
**Статус:** ✅ **ГОТОВО**
